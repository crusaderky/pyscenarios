name: test_env
channels:
  - anaconda
dependencies:
  - python=3.7
  - flake8
  # setup.py needs to import the whole module in order to calculate the V matrix
  - dask
  - numba
  - numpy
  - pandas
  - scipy
