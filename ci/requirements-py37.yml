name: test_env
channels:
  - anaconda
dependencies:
  - python=3.7
  - coveralls
  - dask
  - numba
  - numpy
  - pandas
  - pytest
  - pytest-cov
  - scipy
  # linter tools (only in the latest version of Python)
  - flake8
  - mypy
