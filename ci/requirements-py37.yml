name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.7
  - coveralls
  - dask
  - flake8
  - numba
  - numpy
  - pandas
  - pytest
  - pytest-cov
  - scipy
  # linter tools (only in the latest version of Python)
  - flake8
  - mypy
