name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.8
  - coveralls
  - dask
  - numba
  - numpy
  - pandas
  - pytest
  - pytest-cov
  - scipy
  # linter tools (only in the latest version of Python)
  - flake8
  - mypy
